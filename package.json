{
  "devDependencies": {
    "stylus": "0.52.0"
  },
  "scripts": {
    "test": "npm run test-1 && npm run test-2",
    "test-1": "stylus src.styl --out res.css --include test/inner --resolve-url",
    "test-2": "stylus src.styl --out res-2.css --include test/inner"
  }
}
